<%= form_for(ultimate) do |f| %>
  <% if ultimate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ultimate.errors.count, "error") %> prohibited this ultimate from being saved:</h2>

      <ul>
      <% ultimate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
